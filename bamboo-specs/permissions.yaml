---
version: 2
deployment:
  name: text-encoding - deploy
deployment-permissions:
  - groups:
      - extensions-developers
    permissions:
      - view
environment-permissions:
  - npmjs:
      - groups:
          - extensions-developers
        permissions:
          - view
          - deploy
